= message_block :on => :organization
%h1 Manage pantries

= link_to "Add pantry", new_authengine_organization_path

%h2 Active organizations
= render :partial => 'organizations',
         :locals => {:organizations => @active_organizations, :name => 'active_organizations'}

%h2 Inactive organizations
= render :partial => 'organizations',
         :locals => {:organizations => @inactive_organizations, :name => 'inactive_organizations'}
