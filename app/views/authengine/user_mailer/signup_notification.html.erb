<%=h @user.firstName %> <%=h @user.lastName %>

An account has been created so that you can access the <%= APPLICATION_NAME || "database" %>.

Please visit this url to activate your account:

<%= @url %>

You will be asked to select a login name and password.

<%= APPLICATION_NAME || "database" %> administrator
